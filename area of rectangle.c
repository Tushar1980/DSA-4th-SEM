#include <stdio.h>
#include <stdlib.h>

int main()
{
    int l,b,Area;
    printf("Enter the values of l and b respectively\n");
    scanf("%d%d",&l,&b);
    Area = l * b;
    printf("Area of rectangle = %d",Area);
    return 0;
}
